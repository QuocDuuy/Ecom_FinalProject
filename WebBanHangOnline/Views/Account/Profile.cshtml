@model WebBanHangOnline.Models.CreateAccountViewModel
@{
    ViewBag.Title = "Profile";
}
<link rel="stylesheet" type="text/css" href="~/Content/assets/styles/main_styles.css">
<!-- Slider -->
<style>
    .news_section_container {
        margin-top: 180px;
    }

    .row-item-new {
        margin-bottom: 20px;
    }

    .title-news {
        font-size: 24px;
        border-bottom: 1px solid;
        padding: 15px 0;
        margin-bottom: 25px;
    }

    .img-box img {
        max-width: 100%;
        max-height: 100%;
    }
</style>
<div class="container news_section_container">
    <div class="row">

        <div class="col-md-12">
            @using (Html.BeginForm("PostProfile", "Account"))
            {
                <h2>Thông tin cá nhân</h2>
                <br>
                @Html.ValidationSummary(false)
                @Html.AntiForgeryToken()
                <div class="form-group">
                    <label for="exampleInputEmail1">Full Name</label>
                    <input type="text" class="form-control" aria-describedby="emailHelp" name="FullName" value="@Model.FullName">
                </div>

                <div class="form-group">
                    <label for="exampleInputEmail1">Username</label>
                    <input type="email" class="form-control" aria-describedby="emailHelp" name="UserName" value="@Model.UserName" readonly>
                </div>

                <div class="form-group">
                    <label for="exampleInputEmail1">Phone</label>
                    <input type="text" class="form-control" aria-describedby="emailHelp" name="Phone" value="@Model.Phone">
                </div>

                <div class="form-group">
                    <label for="exampleInputEmail1">Email</label>
                    <input type="email" class="form-control" aria-describedby="emailHelp" name="Email" value="@Model.Email" readonly>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-success">Cập Nhật</button>
                </div>
            }

            <!--<br>
            <h2>Lịch sử đơn hàng</h2>
            <br>

            <table class="table">
                <thead>
                    <tr>
                        <th>Mã đơn hàng</th>
                        <th>Ngày đặt hàng</th>
                        <th>Tên khách hàng</th>
                        <th>Số điện thoại</th>
                        <th>Địa chỉ</th>
                        <th>Sản phẩm</th>
                        <th>Tổng tiền</th>-->
                        <!-- Add other columns as needed -->
                    <!--</tr>
                </thead>
                <tbody>-->
  @*                  @foreac (var order in Model.OrderViewModels)-->
                    <!--{
                        <tr>
                            <td>@order.Code</td>
                            <td>@order.OrderDate.ToString("dd/MM/yyyy")</td>
                            <td>@order.CustomerName</td>
                            <td>@order.Phone</td>
                            <td>@order.Address</td>
                            <td>
                                @foreach (var productName in order.Products)
                                {
                                    <span>@productName</span><br />
                                }
                            </td>
                            <td>@order.TotalAmount</td>-->
                            <!-- Add other columns as needed -->
                        <!--</tr>
                    }*@
                <!--</tbody>
            </table>-->
        </div>
    </div>
</div>